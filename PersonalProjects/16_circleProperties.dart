/**
 * Create a Dart class to represent a circle with properties for radius.
 */

// create class
class Circle {

  double radius;
  // double diameter;

  Circle(this.radius);

  // Circle.circleRadius(this.radius) : diameter = 0;
  // Circle.circleDiameter(this.diameter) : radius = 0;

}